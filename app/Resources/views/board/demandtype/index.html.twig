{% extends '::board/base.html.twig' %}

{% block body %}
    <div id="main" role="main">
        <div id="ribbon">
            <ol class="breadcrumb">
                <li>Home</li><li>Demands</li><li>Types</li>
            </ol>
        </div>

        <div id="content">
            <table class="table table-bordered">
                <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    {% if 'ROLE_HR' in user.roles %}
                        <th>Actions</th>
                    {% endif %}
                </tr>
                </thead>
                <tbody>
                {% for demandType in demandTypes %}
                    <tr>
                        <td>{{ demandType.id }}</td>
                        <td>{{ demandType.name }}</td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('demandtype_show', { 'id': demandType.id }) }}">show</a>
                                </li>
                                <li>
                                    <a href="{{ path('demandtype_edit', { 'id': demandType.id }) }}">edit</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}

                {% if 'ROLE_HR' in user.roles %}
                    <tr>
                        <td colspan="6">
                            <a class="btn btn-lg btn-primary" href="{{ path('demandtype_new') }}">Create a new demand type</a>
                        </td>
                    </tr>
                {% endif %}

                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
